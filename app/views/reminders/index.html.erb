<script>
	
$(document).ready(function() {
		$.ajax({
	  		url: "<%= user_mailer_weekly_url %>",
	  		data: "customer_id=" + <%= @customer.id %> + "&newsletter_id=" + "<%= @newsletter.id %>" + "&section=reminders" + "&control=yes",
	  		success: function(result) {
	  			$('#sample').html(result);
	  	}});		

})	
	
</script>